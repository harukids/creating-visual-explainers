# OpenAI API key（サーバー側のみ — ブラウザには出さない）
OPENAI_API_KEY=sk-...

# 課金前のUI確認用（サーバー側のみ。リクエストで切り替え不可）
# DUMMY_MODE=true

# Slack通知先（Incoming Webhook URL）
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# 任意：公開URLで悪用されにくくする簡易パスワード（設定したらフロントから同じ値を送る）
# ACCESS_CODE=my-secret-word
